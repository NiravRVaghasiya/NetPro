# NetPro v0.1-alpha Scaffold — Progress Log

> Durable, git-tracked copy of the subagent-driven-development progress ledger.
> The live ledger lives at `.git/worktrees/v0.1-alpha-scaffold/sdd/progress.md` (ephemeral,
> local-only, tied to this worktree). This file is the persisted snapshot, updated
> alongside it after every task closes out.

Plan: [`docs/superpowers/plans/2026-08-30-v0.1-alpha-scaffold.md`](2026-08-30-v0.1-alpha-scaffold.md)
Branch: `v0.1-alpha-scaffold` (worktree at `.worktrees/v0.1-alpha-scaffold`)
Execution mode: subagent-driven-development — fresh implementer subagent per task, task-scoped reviewer after each, findings verified via live command execution (not just trusting reports).

## Status: 9 of 15 tasks complete and reviewed. Task 10 implemented (DONE_WITH_CONCERNS) but NOT YET REVIEWED — holding here per explicit user request until told to resume.

## Tasks

- **Task 1** — Root workspace, Turborepo, base TypeScript config: complete (commits `d71dee7..29db50a`, review clean). Note: TypeScript resolved to 7.0.2 (major jump from 5.x) — reviewer flagged as forward-looking watch item for later tasks that actually run `tsc` against real `.ts` files.
- **Task 2** — Shared config package (`packages/config`): complete (commits `29db50a..c115183`, review clean after fix). Critical finding during review: `typescript-eslint` hard-refuses TS 7.x (which Task 1's unpinned install pulled in). Plan amended (`9aa3672`) to pin `typescript@^5` everywhere; fix commit `c115183` re-pinned to `5.9.3` and reconciled the tree. Re-review confirmed via live ESLint execution: works cleanly, no `--legacy-peer-deps` needed.
- **Task 3** — Root Vitest config, docs, license: complete (`c115183..2b3f31f`, review clean). Vitest resolved to `4.1.11` (unpinned) — reviewer confirmed `test.projects` config key is correct for v4 and verified via `--version`/`--help`.
- **Task 4** — `packages/db` dual-dialect Drizzle schema: complete (`2b3f31f..04198a2`, review clean). Minor notes carried forward (not defects, plan-mandated/inherited): JSON columns are auto-parsed objects in SQLite (`{mode:'json'}`) but raw strings in Postgres (plain `text`) — future consumers must handle this dialect difference; `searchIndex.updatedAt` has no `$defaultFn` unlike other timestamp columns.
- **Task 5** — `packages/core` module boundaries: complete (`04198a2..eaf8ea9`, review found "Critical" missing `engines` field). Investigated: Tasks 2 and 4's package.json templates already omitted `engines` too and were approved without issue — a plan-text inconsistency (Global Constraints wording vs. actual task templates), not an implementer defect. Resolved by clarifying the constraint (`6e80456`): `engines` belongs on root + `apps/cli` + `apps/web` only, not on internal `packages/*` libraries. No code fix needed.
- **Task 6** — `packages/ui` component scaffold: complete (`6e80456..b48913d`, review found "Important" typescript-version finding). Verified as a reviewer false positive: root's own approved `package.json` has `"typescript": "^5.9.3"` (not literally `"^5"`) — "pinned to `^5`" means the install specifier is `typescript@^5`, npm writes the real resolved caret range. No fix needed.
- **Task 7** — `apps/cli` bootstrap (commander root program): complete (`b48913d..ae21984`, review clean after 3 fix rounds). History: (1) `commander`/`@netpro/config` were never installed as real deps, worked by accident via a hoisted dev transitive package — fixed. (2) Fixing that revealed a genuine plan bug: shebang duplicated between `index.ts` source and the tsup `banner`, crashing the build — plan corrected to banner-only. (3) `commander@latest` resolved to v15 requiring Node `>=22.12` vs the project's `>=20` floor — re-pinned to `v14.0.3` (`engines >=20`, exact match). All fixes independently verified via live command execution across multiple re-reviews.
- **Task 8** — `apps/cli` seven stub commands + encrypted-file keychain: complete (`a3fa257..5b36ed0`, review clean). Crypto correctness independently verified via tamper tests (bit-flip on ciphertext/authTag correctly rejected, IVs confirmed random per write). Noted for future feature work (not fixed, inherited from the brief's own reference code, low-risk for a scaffold): `Keychain` has no read-modify-write locking (last save wins on concurrent writes); 16-byte IV instead of the NIST-recommended 12-byte (not a vulnerability, just convention).
- **Task 9** — `apps/web` Next.js bootstrap, Tailwind, landing page: complete (`5b36ed0..3e37373`, review clean). Separate housekeeping fix (`20c27e6`): root `package.json` was missing a `"packageManager"` field, which broke `turbo` repo-wide (discovered via this task's build verification) — fixed and plan amended. Versions resolved: `next@16.3.3`, `react@19.2.8`, `tailwindcss@3.4.19` (correctly v3). Follow-up noted (non-blocking, reviewer's explicit call): `apps/web/types/netpro-config.d.ts` is a local ambient-type workaround for `packages/config/tailwind` lacking real TS types; architecturally the fix belongs in `packages/config` itself (would also affect `@netpro/config/eslint` the same way) — candidate for a small follow-up task after the main plan completes.
- **Task 10** — `apps/web` DB client, Auth.js, middleware, health route: **in progress**. Modifies `packages/db`'s two schema files to add the four standard Auth.js adapter tables (`users`, `accounts`, `sessions`, `verificationTokens`) — never in the original DB blueprint, added deliberately here to close a real gap between the source blueprints (the Security doc assumes these tables exist; the DB doc never defined them). Flagged known risk going in: whether `DrizzleAdapter(conn.db, ...)` typechecks cleanly against the dual-dialect discriminated union from `createDb()` — genuinely uncertain, not a proven fact, called out explicitly in the dispatch.
- **Task 10** — `apps/web` DB client, Auth.js, middleware, health route: implementer reported **DONE_WITH_CONCERNS** (commit `133fe8a..48a3a6a`), **not yet reviewed** — paused here on explicit user request. The flagged `DrizzleAdapter` typing risk materialized as predicted (`conn.db`/`conn.schema` unions don't structurally match `@auth/drizzle-adapter`'s `DefaultSchema<SqlFlavor>`); fixed by narrowing on `conn.dialect` before calling `DrizzleAdapter`, no unsafe casts, matching the pre-approved fallback in the dispatch. A second, brief-unlisted issue also surfaced: TS2742 "inferred type cannot be named" from `tsconfig.base.json`'s `declaration: true` checking `next-auth` v5 beta's internal types even under `noEmit` — fixed by setting `declaration: false` in `apps/web/tsconfig.json` only (other packages unaffected). Both `packages/db` and `apps/web` typecheck clean per the implementer; needs independent review verification before marking complete.
- **Tasks 11-15** — not started: placeholder routes, shadcn/ui Button, Docker, CI, final verification.

## Resuming

When told to continue: dispatch Task 10's task-scoped reviewer (base `133fe8a`, head `48a3a6a`), paying particular attention to the `DrizzleAdapter` narrowing fix and the `declaration: false` tsconfig change (outside the brief's literal file list, hence `DONE_WITH_CONCERNS`) — verify both live, not just by reading the report. Then proceed task-by-task through 15 as before.

## Cross-cutting lessons (apply to remaining tasks)

- Unpinned `npm install <pkg>` on a fast-moving package can resolve to a version that breaks compatibility with something else in the tree (TypeScript 7.x vs `typescript-eslint`; `commander@15` vs the Node `>=20` floor) — or, inversely, can dedupe against a stale transitive version already in the lockfile instead of fetching current (`commander` initially landed on `4.1.1` this way). Every implementer dispatch now carries an explicit instruction to verify resolved versions live, not just trust that `npm install` exiting 0 means correctness.
- Never accept a subagent's self-report at face value for anything security- or build-critical — every fix in this log was confirmed via independent re-execution (running ESLint against a real file, tamper-testing the crypto, re-running `tsc`/`vitest`/`next build` directly) before being marked approved.
- Two genuine plan-authoring bugs were found only through implementation (duplicate shebang; missing `packageManager` field) — both fixed retroactively in the plan document itself, not just patched in code, so a future rebuild from the plan doesn't repeat them.
