# NetPro v1.5 Phase 7 implementation — CRM Tracking & Follow-ups

See the [design spec](../specs/2026-09-06-v1.5-phase7-crm-tracking-followups-design.md).

## Implementation checklist

- [x] Schema indexes for the CRM read/write paths (both dialects) generated as
      an additive migration; migration tests still green.
- [x] Core `crm` module: validation vocabularies and limits, `logInteraction`
      with stat/score recomputation (Deep Dive §2.6 formula, stored 0–1),
      `listInteractions`, `getContactTimeline`, follow-up CRUD with
      due-today/overdue/upcoming views, snooze, recurrence re-arm, and
      activity-log writes; adversarial tests on the migrated SQLite fixture.
- [x] CLI `netpro track` (replaces stub): `log`, `add --met-at --follow-up`,
      `list` views, `done`, `snooze --for`; selector reuse from core/ai;
      duration parsing; `--json`; output/behavior tests.
- [x] Web API: `GET /api/contacts`, `GET /api/contacts/[id]`,
      `POST /api/interactions`, `GET/POST /api/follow-ups`,
      `PATCH /api/follow-ups/[id]`; validation + 404 tests behind the owner
      boundary.
- [x] Web pages: `/contacts` CRM table (last touch, count, score, next
      follow-up, due summary, pagination) and `/contacts/[id]` timeline with
      log-interaction / follow-up client panel; render tests.
- [x] Dashboard follow-up-due surface (small strip linking to `/contacts`).
- [x] README v1.5 section; docs touch-ups; full verification (lint,
      typecheck, test, build, zero warnings) recorded below.

## Verification record

Implemented on branch `arena/01a07859-netpro` (2026-09-06) and delivered in
the v1.5 pull request alongside Phase 8.
Every gate green at commit time:

- **Typecheck** — `npm run typecheck`: 8/8 tasks successful.
- **Lint** — `npm run lint`: clean (only the benign, pre-existing turbo
  "no output files found for task @netpro/core#build" notice).
- **Tests** — `npm test`: `@netpro/core` **257 passed**; `@netpro/cli`
  **104 passed + 2 skipped** (14 files); `@netpro/web` **120 passed**
  (22 files); `@netpro/db` **28 passed + 7 skipped** (the skipped set is the
  live PostgreSQL integration suite that runs in CI); `@netpro/ui` **1 passed**.
- **Build** — `npm run build`: exit 0, zero warnings; the Next.js route
  manifest lists all Phase 7 routes — `/api/contacts`, `/api/contacts/[id]`,
  `/api/interactions`, `/api/follow-ups`, `/api/follow-ups/[id]`, `/contacts`,
  and `/contacts/[id]`.

Relationship scoring is stored 0–1 and recomputed on every `logInteraction`
(one indexed SELECT + pure JS). Hand-verified vectors from the design spec
reproduce exactly (meeting today → 48; email sent+received today → 75; note
40 days old → 40; ten notes today → 69; five types 100 days old → 35; three
mixed today → 83). Migration `0002` adds nine portable, non-partial indexes
across both dialects.

(The README's milestone/version status was updated once, at the completion of
v1.5 in Phase 8, so it reflects Phases 7 and 8 together.)
