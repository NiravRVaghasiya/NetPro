# NetPro v1.0 Phase 1 (Import, Enrichment & Export) — Progress Log

> Durable, git-tracked copy of the subagent-driven-development progress ledger.
> The live ledger lives at `.git/worktrees/v1.0-phase1-import-export/sdd/progress.md`
> (ephemeral, local-only, tied to this worktree). This file is the persisted
> snapshot, updated alongside it after every task closes out — same pattern
> used for the v0.1-alpha scaffold's progress log.

Plan: [`docs/superpowers/plans/2026-08-31-v1.0-phase1-import-enrichment-export.md`](2026-08-31-v1.0-phase1-import-enrichment-export.md)
Design spec: [`docs/superpowers/specs/2026-08-31-v1.0-phase1-import-enrichment-export-design.md`](../specs/2026-08-31-v1.0-phase1-import-enrichment-export-design.md)
Branch: `v1.0-phase1-import-export` (worktree at `.worktrees/v1.0-phase1-import-export`)
Execution mode: subagent-driven-development — fresh implementer subagent per task, task-scoped reviewer after each, findings verified via live command execution (not just trusting reports).

## Status: 15 of 15 tasks complete — v1.0 Phase 1 is done. Tasks 1-9 landed in PR #8 (`master` @ `12b5825`); tasks 10-15 were executed on 2026-09-06 on branch `arena/01a07638-netpro` (`432b719..cc300b8`), with the plan-correction notes recorded per task below. Full `npm run lint && npm run typecheck && npm run test && npm run build` is green repo-wide (0 errors, 0 warnings; 81 tests).

## Tasks

- **Task 1** — LinkedIn CSV parsing and contact normalization: complete (`ec1817f..ae07d3e`, review clean). 15 tests (4 CSV parsing + 11 normalization) — the plan's own "9 tests" prediction was a miscount on the plan author's part, not reproduced by the implementer, who correctly matched the actual test code specified. Minor note carried forward (inherited from the plan's own `mergeContacts` spec, not an implementer gap): `mergeContacts` doesn't reconcile `seniority`/`firstName`/`lastName`, only `email`/`company`/`role`/`location`/`fullName`.
- **Task 2** — Import pipeline (dedup + persist to DB): complete (`ae07d3e..1d23a3a`, review clean). First task to touch the database. The plan had two real gaps here, both fixed retroactively (commit `4384349`, docs-only): a missing dependency-install step for `@netpro/db`/`drizzle-orm`/`better-sqlite3` (the implementer correctly added them anyway), and an unused `migrate` import in the specified test code. **Confirmed (not just predicted) that `packages/db`'s `SqliteConn | PgConn` union needs `conn.dialect === 'sqlite'` narrowing before `.select()`/`.insert()`/`.update()` typecheck** — the plan's unnarrowed reference code failed with `TS2349`. Fixed with two unconditionally-terminating branches (proven pattern from the v0.1-alpha scaffold's `auth.ts`, now reused here). Reviewer verified the narrowing is genuine (self-enforcing via TS control-flow) and the two branches are currently byte-identical with no drift. Carried forward: no test exercises the Postgres branch, so future drift between the two hand-duplicated branches wouldn't be caught by tests — inherent to this project's scope (no live Postgres instance anywhere yet), not a defect.
- **Task 3** — Enrichment types, rate limiter, and cache: complete (`1d23a3a..6bd4255`, review clean). Same dialect-narrowing requirement hit again in `getCached`/`setCached` (`cache.ts`), confirming this is now an established, expected pattern for any `packages/core` code touching `conn.db`. The implementer added two TypeScript overload signatures to `serializePayload` (runtime body unchanged) to avoid an `as` cast at a dialect-narrowed call site — the reviewer independently verified this against `drizzle-orm`'s actual `.d.ts` files (not just the implementer's claim) and confirmed the Postgres-branch overload is genuinely load-bearing (without it, `unknown` isn't assignable to the pg `text` column's `string` insert type); the SQLite-branch overload turned out to be harmless-but-not-strictly-required, a minor over-statement in the report rather than a code defect. `serializePayload`/`deserializePayload` (the JSON-column dialect-asymmetry fix — SQLite auto-parses `{mode:'json'}`, Postgres stores raw `text`) are genuinely tested for both dialects via pure, DB-free unit tests. Carried forward: same as Task 2, `getCached`/`setCached`'s Postgres path is unit-tested for serialization but not integration-tested end-to-end.

- **Task 4** — Hunter.io enrichment provider: complete (`8f85680..5c40fdf`, review clean). First provider task — pure HTTP logic, no database access, so no dialect-narrowing question here (confirmed by the reviewer as expected). Test suite genuinely exercises both the "domain already known" (1 fetch) and "must resolve via company name" (2 fetches) paths with value-specific assertions tied to the mocked `score`/`verification.status`, not just existence checks. Two Minor notes carried forward, both inherited from the plan's own reference code (not implementer deviations): `firstName`/`lastName` are always derived from `fullName.split(' ')` rather than using `EnrichableContact`'s own `firstName`/`lastName` fields (breaks on single-word or double-spaced names); `findDomain`'s internal `domain-search` failure swallows the HTTP status into a generic "could not resolve domain" error rather than surfacing the code, unlike the `email-finder` failure path which does surface it. Worth revisiting when Tasks 5/6 (PDL, Clearbit) are built, in case the same name-splitting pattern gets copied forward.

- **Task 5** — People Data Labs enrichment provider: complete (`e7dc211..9f57313`, review clean; test-coverage fix `5a6d658`). Same pure-HTTP shape as Task 4, no dialect narrowing. Reviewer did a genuinely useful cross-check: verified PDL's seniority-map output values are all valid members of this project's real `Seniority` type (`packages/core/src/import/normalize.ts`), not just internally consistent. One real gap found and closed directly (small enough not to need a subagent round): the multi-level `job_title_levels` reduction (pick the highest rank regardless of array order) had been hand-verified correct by the reviewer but had zero test coverage — added a test asserting `['senior','director','manager']` → `'director'`. Two purely cosmetic Minor nits (a no-op `?? undefined`, a harmless self-comparison on the reduce's first iteration) left as-is — zero behavior impact.

- **Task 6** — Clearbit enrichment provider: complete (`bdafb47..3565abe`, review clean). Last of the three enrichment providers. Correctly held the company-level-only field scope (no person fields leaked in from the Hunter/PDL pattern it was built alongside) and both the domain-lookup and name-fallback branches are genuinely distinct test cases. One cosmetic Minor note (inherited from the plan's own test spec): the domain-lookup test doesn't independently assert the request URL the way the name-fallback test does — asymmetric rigor, not a real gap, left as-is.

- **Task 7** — Enrichment pipeline orchestrator: complete (`2ba7bca..252fdb2`, review clean). Last task in Phase 2. Dialect narrowing confirmed a THIRD time (`persistEnrichment`), fully establishing the pattern. **Found and fixed a systemic plan defect during this task**: Drizzle's typed `.select()` generates an explicit column list matching the full imported schema, so a test fixture's partial `CREATE TABLE contacts` (fewer columns than the real 33-column schema) fails at runtime the instant any `.select()` runs — regardless of which columns the test actually needs. Only Task 2's fixture had the full column list; Tasks 7, 9, 10, 11, 12, 13, and 14 all had partial ones and would have hit this identically, one at a time, as each was implemented. Fixed proactively across all of them in one pass (commit `8aa3ce3`, docs-only) instead of letting each subsequent implementer rediscover it independently. Also confirmed (by having the reviewer independently read Task 8's actual plan text, not just trust the claim) that leaving `packages/core/src/index.test.ts` failing was correctly deliberate — Task 8 fixes it as already planned, not an implementer oversight.

**Phase 2 (Enrichment Core) is complete.**

- **Task 8** — CSV export: complete (`0ca26e3..f549810`, review clean). Only task in Phase 3. Delegates to `papaparse` for formatting (no hand-rolled quoting). Closed out `packages/core/src/index.test.ts`'s expected failure from Task 7 by wiring the real `import`/`enrichment`/`export` modules into the public barrel. **Found and fixed another real plan defect**: my barrel-export code for `index.ts` silently dropped the `analytics` scaffold module (listed 6 of the original 7) — the file was never deleted, just omitted from the re-export list. Fixed in the plan (`f30b763`) and with a minimal 2-line follow-up commit (`f549810`), both independently verified via direct `git show` on the actual commits rather than trusting either implementer's report. Full `packages/core` suite: 53 tests, all passing.

**Phases 1-3 (Import, Enrichment, Export Core) are all complete — `packages/core`'s new business logic is fully built and tested.**

- **Task 9** — CLI `import` command, real implementation: complete (`135e863..f9419ac`, review clean). First task in Phase 4, first task in this plan to touch `apps/cli`. `@netpro/core`/`@netpro/db` added as real dependencies of `apps/cli` for the first time. **Found and fixed a genuinely important bug beyond the brief's scope**: `apps/cli/tsup.config.ts`'s `external` array was missing `'pg'` — the CLI built with zero errors but crashed at runtime (`Dynamic require of "events" is not supported`) the instant any command dynamically imported `@netpro/db`, because `pg`'s CJS internals got bundled into the ESM output. Independently reproduced by the reviewer (temporarily reverted the fix, confirmed the exact crash and dist size; restored, confirmed clean behavior and matching dist size) — a real, durable fix to shared bundler config, not something Tasks 10-11 need to repeat since they build against the same already-fixed `tsup.config.ts`.

- **Task 10** — CLI `enrich` command: complete (`432b719`). Followed the brief (dialect-narrowed `listContacts` per the now-standard pattern; `Keychain` keys; `EnrichmentPipeline.enrichBatch`). Two documented deviations: (a) the brief's reference test assumed `Keychain.get()` naturally resolves to null in a fresh test environment — implemented instead with the repo's established `vi.stubEnv('HOME', tempHome)` isolation (the scaffold review had explicitly flagged the silent-failure risk of the other approach), so a developer machine with real enrichment keys can never leak them into the test; (b) **plan defect found and fixed**: the brief's own user-facing message and the design spec's key-setup flow both reference `netpro config set enrichment.<provider> <key>`, but no `config` command was ever registered — the scaffold built only the `Keychain` class (`config.ts` was flagged "unused pass-through" in the scaffold review). Without it, `enrich` had no usable key-setup path, so the command was added (9th top-level command: `config set|get|delete|list`, thin wrapper over the existing Keychain static API, 4 tests). `cli.test.ts` asserts 9 commands, not the brief's 8.
- **Task 11** — CLI `export` command: complete (`c8ea27a`). Brief as written; dialect-narrowed select. Verified `--format vcard` fails loudly (stderr + exit 1), not a silent no-op.
- **Task 12** — Web import page + `POST /api/import`: complete (`5e0c0df`). Brief as written for route and page. Two necessary additions: (a) `apps/web/vitest.config.ts` gained the `@` → workspace-root alias (mirrors tsconfig paths) — the brief's `vi.mock('@/lib/db')` cannot resolve without it; the scaffold's only web test used relative imports, so the gap was latent; (b) `middleware.ts` gained an `API_ROUTES` category returning 401 JSON for `/api/import|export|enrich`, per the Security & Auth Architecture blueprint's §1.4 API Route Protection pattern (its example list already presaged `/api/enrich`), which the scaffold's simplified middleware never carried over — unauthenticated requests could otherwise read/write the personal contact DB.
- **Task 13** — Web contacts list + `GET /api/export`: complete (`7e0d09a`). Brief as written, both surfaces dialect-narrowed.
- **Task 14** — Web `POST /api/enrich` + env vars in `apps/web/.env.example`: complete (`0beb7d8`). Brief as written; the brief's `contactId!` non-null-assertion was replaced with an optional-parameter narrowing (same behavior).
- **Task 15** — Full verification, docs, final commit: complete (`cc300b8` + docs commit). **Found the phase's biggest real gap via Step 3's end-to-end run**: a fresh database had *no schema at all* — `packages/db/migrations/{sqlite,postgres}` contained only `.gitkeep`, and every unit test creates its own DDL (the plan's self-review had accepted this as "a real migration system would eliminate the need entirely"), so `netpro import|enrich|export` all crashed with `no such table: contacts` on a first-run install — which would have failed three of the design spec's seven success criteria. Fixed per the DB & Pipeline Deep Dive blueprint's migration strategy: generated initial migrations for both dialects (14 tables), added CLI `openDb()` (createDb + idempotent journal-based migrate, wired into the three actions; `createDb` kept a dynamic import so `--help`/`config` never load native modules) and web `instrumentation.ts` (applies at server start; the pg migrator is async so module-scope `lib/db.ts` can't host it). The blueprint's "with user confirmation" was dropped deliberately (cron-friendly CLI, no breaking migrations this phase) — documented in the commit. Two implementation bugs found and fixed during verification: the first instrumentation version's *static* `node:path`/`node:fs` imports crashed Next's Edge context (the same bug class as the scaffold's middleware/better-sqlite3 incident — all node-only code is now behind the `NEXT_RUNTIME` guard + dynamic-import boundary), and drizzle-orm 0.45.2's pg migrator no longer accepts a `schema` config key. Also removed an unused `migrate` import in `packages/core/src/import/pipeline.test.ts` (inherited from Task 2's brief test code, the plan's only remaining lint warning).
  - **Verification results:** lint/typecheck/test/build all green repo-wide (81 tests). CLI E2E: fresh DB → import reports `4 imported (1 merged)` for a 5-row fixture with an in-file email duplicate; re-import reports `0 imported (5 merged)` (dedup works end-to-end, not just in unit tests); `enrich --source all` reports `Enriched 0 of 4 contacts / 12 skipped: no API key configured` (the CORRECT no-key outcome); `export` produces a valid CSV with `Vercel Inc.` normalized to `Vercel`. Web E2E: dev server migrates the schema at startup (15 tables in `apps/web/netpro.db`), `/api/health` 200 (regression), unauthenticated `/api/import|export|enrich` → 401 JSON, `/import` page → 307 to `/login`, landing 200. **Disclosed limitation (per the plan's own fallback clause):** live GitHub OAuth sign-in was not possible in the execution environment (no `GITHUB_CLIENT_ID`/`SECRET`), so the interactive browser flows (drag-and-drop upload rendering, contacts table with real data, in-browser CSV download) were verified at the route-handler level (unit tests), the build's route list, and the auth-gate level (401/307) — not via a signed-in browser session.
  - Environment note: the sandbox blocks TLS to nodejs.org/objects.githubusercontent.com, so `better-sqlite3` was built from source against the locally installed Node 22.22.3 headers (`node-gyp rebuild --nodedir=/usr/local`) rather than via prebuilds — an execution-environment detail, no repo change.

## A second lesson on plan-document hygiene, beyond the fence-parity one from the scaffold plan

Two real defects surfaced in THIS plan during implementation so far, both caught by implementers reading the actual codebase rather than blindly following the brief: a systemic partial-DDL bug (Task 7) and a silently-dropped module in a barrel-export rewrite (Task 8). Both are the same underlying failure mode — hand-retyping a list of things (columns, module names) from memory/context rather than copying it verbatim from a known-good source, and silently dropping an entry. When a future plan needs to reproduce a list that already exists correctly elsewhere in the codebase or an earlier task, copy it exactly rather than retyping it from memory.

## Cross-cutting pattern confirmed in this session

The `conn.dialect === 'sqlite'` narrowing requirement — first discovered in the v0.1-alpha scaffold's `apps/web/lib/auth.ts` — has now recurred in every single piece of `packages/core` code that queries the database (Tasks 2 and 3, both). This is no longer a "might happen" risk to flag defensively; it's a confirmed, load-bearing characteristic of this codebase's `SqliteConn | PgConn` design. Every remaining task whose brief includes a `conn.db` query (Task 7's pipeline persistence, and the web routes in Tasks 12-14) should expect to need it and should not be surprised when the brief's literal unnarrowed reference code fails typecheck. Tasks 4-6 (the providers) don't touch the database at all, so this doesn't apply to them.
