# v2.0 Phase 1 — Edge provenance — progress

Implemented against `docs/superpowers/plans/2026-09-07-v2.0-implementation-plan.md`.

- Migration `0003` (SQLite + Postgres): provenance columns, indexes, `events` / `event_attendees`.
- Core `packages/core/src/graph`: CRUD, dedupe, confirmation, CSV mutuals, event attendance.
- CLI `netpro edge`, web `/edges` + `/api/edges`, contact “Also met at…”.
- Import pipeline surfaces pending mutual-network candidates; never auto-confirms.

Verification: `npm test` across db/core/cli/web after implementation.
